# Copy this file to .env and fill in your keys for local development.
# .env is gitignored — never commit real keys.
#
# In GitHub Actions these are set as repository secrets (Settings → Secrets → Actions).
# Locally this file is loaded automatically by conftest.py via python-dotenv.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...